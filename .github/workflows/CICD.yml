name: Json variables substitution

on: [push]
  
    
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: microsoft/variable-substitution@v1
      with:
          files: 'appsettingsone.json'
      env:
       iisSettings.windowsAuthentication: ${{ secrets.WINDOWSAUTHENTICATION }}
       iisSettings.anonymousAuthentication: ${{ secrets.ANONYMOUSAUTHENTICATION}}
    
    - run: echo "🎉 The job was automatically triggered by a ${{ secrets.WINDOWSAUTHENTICATION }}"
    - run: echo "🎉 The job was automatically triggered by a ${{ secrets.ANONYMOUSAUTHENTICATION}}"
